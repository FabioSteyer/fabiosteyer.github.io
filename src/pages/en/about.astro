---
import Base from '../../layouts/Base.astro';
import { Image } from 'astro:assets';
import fotoFabio from '../../assets/foto-fabio-steyer.jpg';

const evidence = [
  { skill: 'Python', proof: 'Production pipelines at Cavator; both projects under Projects', level: 'publicly verifiable' },
  { skill: 'Document parsing', proof: 'invoice-quote-reconciliation, measured against a ground truth', level: 'publicly verifiable' },
  { skill: 'Concurrency and data integrity', proof: 'concurrent-file-writes, measured with and without coordination', level: 'publicly verifiable' },
  { skill: 'Test discipline', proof: '38 tests across both projects, including multi-process runs', level: 'publicly verifiable' },
  { skill: 'Scientific data analysis', proof: 'B.Sc. thesis, spectroscopy and model fitting', level: 'publicly verifiable' },
  { skill: 'OCR', proof: 'Around 3,000 invoices and quotes at Cavator, not public', level: 'used in practice' },
  { skill: 'LLM integration (Claude, OpenAI)', proof: 'Built into my own pipelines at work; in neither project here', level: 'used in practice' },
  { skill: 'Git', proof: 'Two repositories with a readable history', level: 'used in practice' },
  { skill: 'SQL', proof: 'University projects, still building', level: 'learning' },
];
---

<Base
  lang="en"
  title="About — Fabio Steyer"
  description="Background, skills and the evidence behind them."
  altHref="/ueber-mich/"
>
  <div class="sm:flex sm:items-start sm:gap-8">
    <div class="flex-1">
      <h1 class="text-3xl font-semibold tracking-tight">About</h1>
      <p class="mt-4 leading-relaxed max-w-2xl">
        I am a physics master's student at TU Berlin and have worked since 2025 where data
        analysis meets process automation. What draws me to it is the decision before the code:
        which step needs a script, which needs a language model, and which is better left alone.
      </p>
    </div>
    <div class="doc-frame mt-6 sm:mt-1 w-32 sm:w-40 shrink-0">
      <Image
        src={fotoFabio}
        alt="Portrait photograph of Fabio Steyer"
        width={160}
        densities={[1, 2]}
        class="w-full h-auto"
      />
    </div>
  </div>

  <section class="doc-section" aria-labelledby="evidence">
    <h2 id="evidence" class="doc-section__label">Skills and evidence</h2>
    <div class="doc-section__body">
      <p class="leading-relaxed" style="color: var(--color-muted)">
        Each row states what it rests on. "Learning" means present, but not backed by a project.
      </p>
      <div class="mt-5 overflow-x-auto">
        <table class="doc-table">
          <thead>
            <tr>
              <th>Skill</th>
              <th>Evidence</th>
              <th>Level</th>
            </tr>
          </thead>
          <tbody>
            {evidence.map((e) => (
              <tr>
                <td>{e.skill}</td>
                <td style="color: var(--color-muted)">{e.proof}</td>
                <td>
                  <span class:list={[
                    'doc-level',
                    e.level === 'publicly verifiable' && 'doc-level--proven',
                    e.level === 'learning' && 'doc-level--learning',
                  ]}>{e.level}</span>
                </td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    </div>
  </section>

  <section class="doc-section" aria-labelledby="ai">
    <h2 id="ai" class="doc-section__label">On AI assistance</h2>
    <div class="doc-section__body">
      <p class="leading-relaxed">
        I work with AI tools, including while building this site. The technical decisions, the
        choice of content and its verification are mine.
      </p>
    </div>
  </section>
</Base>
