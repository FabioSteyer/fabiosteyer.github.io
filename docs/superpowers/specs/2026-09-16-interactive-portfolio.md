# Interactive portfolio

Approved direction: retain the existing green, copper, sage palette and local
Newsreader/Karla/JetBrains Mono fonts. Make the projects understandable through
two purposeful interactive scenes, a compact introduction and connected motion.

## Scope

- Astro static site, existing routes and noindex retained.
- Shared DE/EN page components and structured content; identical measured facts.
- Replace broken CSS scroll-timeline animation with optional GSAP ScrollTrigger.
- Ordinary native scrolling; no scroll hijacking, prolonged pinning or autoplay loops.
- Responsive invoice illustration with three selectable synthetic cases:
  price mismatch, split delivery and unit conversion. Compute results locally.
- Four-process illustration with a toggle for unsafe versus coordinated writes.
  Identify it as a simplified illustration; distinguish it from the measured
  repository run (20 expected, 14 lost without coordination, zero lost with it).
- Expandable inline case studies using native details; animate when supported.
- Keyboard operation, reduced motion, no-JavaScript readable final states.
- No new third-party requests, tracking, remote fonts, accounts or private data.
- Update privacy statements to describe locally served JavaScript.
- Master's degree is in progress. Preserve proof levels and measured limits.
- Validate the production build, both languages, 320/390/768/1280px layouts,
  browser interactions, keyboard and accessibility, static fallbacks and payload.

The user authorized autonomous reversible work. Implementation and validation
continue without intermediate approval; irreversible operations need confirmation.
