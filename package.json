{
  "name": "portfolio-website",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "test": "npm run build && node --test tests/*.test.mjs",
    "check": "astro check",
    "preview": "astro preview",
    "preview:no-js": "node scripts/preview-no-js.mjs",
    "astro": "astro",
    "deploy": "powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/deploy-gh-pages.ps1"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "astro": "^7.3.2",
    "gsap": "^3.15.0",
    "tailwindcss": "^4.3.3"
  },
  "allowScripts": {
    "esbuild": true
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^6.0.3"
  }
}
